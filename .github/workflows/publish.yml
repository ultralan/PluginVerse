name: publish

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: publish-runtime
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm run build
        env:
          TMB_PUBLIC_BASE_URL: ${{ vars.TMB_PUBLIC_BASE_URL || format('https://fastly.jsdelivr.net/gh/{0}/{1}@published', github.repository_owner, github.event.repository.name) }}
          TMB_SUPABASE_URL: ${{ vars.TMB_SUPABASE_URL || 'https://ovxaymldzdiatllhjbvx.supabase.co' }}
          TMB_SUPABASE_ANON_KEY: ${{ secrets.TMB_SUPABASE_ANON_KEY || 'sb_publishable_H3ea5scu5VwoJAkA6ZDQtA_WTj-Eycq' }}
          TMB_BUILD_VERSION: 0.1.${{ github.run_number }}

      - name: Verify
        run: pnpm run verify

      - name: Record build
        run: pnpm run record-build
        env:
          TMB_SUPABASE_URL: ${{ vars.TMB_SUPABASE_URL || 'https://ovxaymldzdiatllhjbvx.supabase.co' }}
          TMB_SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.TMB_SUPABASE_SERVICE_ROLE_KEY }}
          TMB_BUILD_VERSION: 0.1.${{ github.run_number }}
          TMB_PUBLIC_BASE_URL: ${{ vars.TMB_PUBLIC_BASE_URL || format('https://fastly.jsdelivr.net/gh/{0}/{1}@published', github.repository_owner, github.event.repository.name) }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REPOSITORY: ${{ github.repository }}

      - name: Publish runtime branch
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          rm -rf .publish
          mkdir .publish
          cp -R dist/. .publish/
          touch .publish/.nojekyll
          cd .publish
          git init
          git checkout -b published
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "发布 Tampermonkey Base 客户端 ${GITHUB_SHA}"
          git push --force "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" published

      # jsDelivr 对分支引用的边缘缓存较顽固，发布后主动 purge。
      - name: Purge jsDelivr cache
        run: |
          set -euo pipefail
          curl -sf "https://purge.jsdelivr.net/gh/${GITHUB_REPOSITORY}@published/client/tampermonkey-base.user.js" >/dev/null
          echo "jsDelivr 缓存已清除。"
